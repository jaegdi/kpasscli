name: Release-RH-Ubi7

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: registry.access.redhat.com/ubi7/ubi:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          repository: jaegdi/kpasscli
          token: ${{ secrets.RELEASE_BUILD }}
          ssh-strict: true
          persist-credentials: true
          clean: true
          fetch-depth: 1
          lfs: false
          submodules: false
          set-safe-directory: true

      - name: Install dependencies
        run: |
          sudo yum -y update
          sudo yum -y install glibc glibc-devel libstdc++ libstdc++-devel libx11-dev

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.16'

      - name: Build
        run: go build -v -o dist

      - name: Run tests
        run: go test -v ./...

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_BUILD }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_BUILD }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./dist/kpasscli
          asset_name: kpasscli
          asset_content_type: application/octet-stream